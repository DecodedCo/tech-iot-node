// Load the Johnhy Five library, and connect
var five = require('johnny-five'),
    board = new five.Board(),
    request = require('request');

// When the board is ready, run our code:
board.on('ready', function() {
  // code goes here









});
